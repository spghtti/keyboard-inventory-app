<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/stylesheets/reset.css" />
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <link rel="shortcut icon" href="#" />
  <title><%= title %></title>
</head>

<body>
  <%- include('sidebar') -%>
  <div class="wrapper">
    <div>
      <h1><%= keyboard.brand.name %> <%= keyboard.name %></h1>
    </div>
    <div>
      <p><%= keyboard.description %></p>
    </div>
    <ul>
      <% if(!keyboard_instances){ %>
      <p>These keyboards aren't available currently.</p>
      <% } else{ %> <% keyboard_instances.forEach((instance) => { %>
      <li>
        <%= instance.keyboard.name %> -- <%= instance.keyboard_switch.name %> (ID: <a href="<%= instance.url %>"><%= instance._id %></a>)
      </li>
      <% }); %> <% } %>
    </ul>
  </div>
</body>

</html>